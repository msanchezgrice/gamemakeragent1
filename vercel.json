{
  "version": 2,
  "builds": [
    {
      "src": "apps/console/package.json",
      "use": "@vercel/next",
      "config": {
        "buildCommand": "cd ../.. && pnpm install && pnpm run build:vercel && cd apps/console && pnpm run build"
      }
    }
  ]
}
